CREATE TABLE stocks
(
	id			INTEGER GENERATED BY DEFAULT AS IDENTITY,
	quarter		INTEGER,
	stock		VARCHAR(255),
	stock_date	VARCHAR(255),
	open		VARCHAR(255),
	high		VARCHAR(255),
	low			VARCHAR(255),
	PRIMARY KEY (id)
);
